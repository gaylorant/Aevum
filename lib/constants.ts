/** localStorage keys — chat session only; wiped on tab close */
export const CHAT_SESSION_CONTEXT_KEY = "session_context";
export const CHAT_HISTORY_KEY = "chat_history";

/** localStorage prefix for Lifebook tools (never wiped on tab close) */
export const LIFEBOOK_STORAGE_PREFIX = "lifebook_";
